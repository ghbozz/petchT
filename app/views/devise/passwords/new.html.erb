<div class="container d-flex-center mb-5" style="margin-top: 9em;">
  <div class="user-form-wrapper fields-area" style="width: 60%;">

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <h2 class="mb-5">Forgot your password?</h2>
      <div class="d-flex-center form-inputs my-3">
        <div style="width: 60%; display: flex; flex-direction: column; align-items: flex-start;">
          <%= f.label :email %>
          <%= f.email_field :email,
                            autofocus: true,
                            autocomplete: "email",
                            class: 'email_recover',
                            placeholder: 'Votre adresse e-mail' %>
          <br>
        </div>
      </div>
      <%= f.submit "Réinitialiser", class: 'btn petch-btn mt-3 mb-5', id: 'recover_btn' %>
    <% end %>

    <div class="d-flex-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

<style>
  .email_recover {
    width: 100%;
    height: 2em;
  }

  #recover_btn {
    margin: 0 auto;
  }
</style>
